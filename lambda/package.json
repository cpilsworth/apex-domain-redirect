{
  "name": "apex-domain-redirect",
  "version": "1.0.0",
  "description": "Adds a www to the apex of a top level domain",
  "main": "index.js",
  "scripts": {
    "build": "zip -r ./lambda.zip index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Chris Pilsworth",
  "license": "Apache2"
}
